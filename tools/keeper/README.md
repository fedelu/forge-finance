Keeper Bot
==========
Minimal keeper that reads LVF positions, computes health, and triggers liquidation when LTV exceeds threshold.

Run
---
- Configure RPC and keypair via env vars.
- Execute `ts-node tools/keeper/keeper.ts`.


