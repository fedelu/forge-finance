{
  "name": "forge-finance",
  "version": "1.0.0",
  "description": "Forge Finance - Solana DeFi Protocol",
  "private": true,
  "scripts": {
    "build": "cd app && npm install && npm run build",
    "start": "cd app && npm start",
    "dev": "cd app && npm run dev"
  },
  "workspaces": [
    "app"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}